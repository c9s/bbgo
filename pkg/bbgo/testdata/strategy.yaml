---
sessions:
  max:
    exchange: max
    envVarPrefix: MAX
  binance:
    exchange: binance
    envVarPrefix: BINANCE

exchangeStrategies:
- on: ["binance"]
  test:
    symbol: "BTCUSDT"
    interval: "1m"
    baseQuantity: 0.1
    minDropPercentage: -0.05
    maxAssetQuantity: 1.1
