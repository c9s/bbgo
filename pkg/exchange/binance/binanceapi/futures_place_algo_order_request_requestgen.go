// Code generated by "requestgen -method POST -url /fapi/v1/algoOrder -type FuturesPlaceAlgoOrderRequest -responseType .FuturesPlaceAlgoOrderResponse"; DO NOT EDIT.

package binanceapi

import (
	"context"
	"encoding/json"
	"fmt"
	"net/url"
	"reflect"
	"regexp"
	"sync"
)

/*
 * AlgoType sets Required parameters
 */
func (f *FuturesPlaceAlgoOrderRequest) AlgoType(algoType AlgoType) *FuturesPlaceAlgoOrderRequest {
	f.algoType = algoType
	return f
}

/*
 * Symbol sets
 */
func (f *FuturesPlaceAlgoOrderRequest) Symbol(symbol string) *FuturesPlaceAlgoOrderRequest {
	f.symbol = symbol
	return f
}

/*
 * Side sets
 */
func (f *FuturesPlaceAlgoOrderRequest) Side(side SideType) *FuturesPlaceAlgoOrderRequest {
	f.side = side
	return f
}

/*
 * OrderType sets
 */
func (f *FuturesPlaceAlgoOrderRequest) OrderType(orderType FuturesAlgoOrderType) *FuturesPlaceAlgoOrderRequest {
	f.orderType = orderType
	return f
}

/*
 * PositionSide sets Optional parameters
 */
func (f *FuturesPlaceAlgoOrderRequest) PositionSide(positionSide PositionSide) *FuturesPlaceAlgoOrderRequest {
	f.positionSide = &positionSide
	return f
}

/*
 * TimeInForce sets
 */
func (f *FuturesPlaceAlgoOrderRequest) TimeInForce(timeInForce TimeInForceType) *FuturesPlaceAlgoOrderRequest {
	f.timeInForce = &timeInForce
	return f
}

/*
 * Quantity sets
 */
func (f *FuturesPlaceAlgoOrderRequest) Quantity(quantity string) *FuturesPlaceAlgoOrderRequest {
	f.quantity = &quantity
	return f
}

/*
 * Price sets
 */
func (f *FuturesPlaceAlgoOrderRequest) Price(price string) *FuturesPlaceAlgoOrderRequest {
	f.price = &price
	return f
}

/*
 * TriggerPrice sets
 */
func (f *FuturesPlaceAlgoOrderRequest) TriggerPrice(triggerPrice string) *FuturesPlaceAlgoOrderRequest {
	f.triggerPrice = &triggerPrice
	return f
}

/*
 * WorkingType sets
 */
func (f *FuturesPlaceAlgoOrderRequest) WorkingType(workingType WorkingType) *FuturesPlaceAlgoOrderRequest {
	f.workingType = &workingType
	return f
}

/*
 * PriceMatch sets
 */
func (f *FuturesPlaceAlgoOrderRequest) PriceMatch(priceMatch PriceMatchType) *FuturesPlaceAlgoOrderRequest {
	f.priceMatch = &priceMatch
	return f
}

/*
 * ClosePosition sets
 */
func (f *FuturesPlaceAlgoOrderRequest) ClosePosition(closePosition bool) *FuturesPlaceAlgoOrderRequest {
	f.closePosition = &closePosition
	return f
}

/*
 * PriceProtect sets
 */
func (f *FuturesPlaceAlgoOrderRequest) PriceProtect(priceProtect string) *FuturesPlaceAlgoOrderRequest {
	f.priceProtect = &priceProtect
	return f
}

/*
 * ReduceOnly sets
 */
func (f *FuturesPlaceAlgoOrderRequest) ReduceOnly(reduceOnly bool) *FuturesPlaceAlgoOrderRequest {
	f.reduceOnly = &reduceOnly
	return f
}

/*
 * ActivatePrice sets
 */
func (f *FuturesPlaceAlgoOrderRequest) ActivatePrice(activatePrice string) *FuturesPlaceAlgoOrderRequest {
	f.activatePrice = &activatePrice
	return f
}

/*
 * CallbackRate sets
 */
func (f *FuturesPlaceAlgoOrderRequest) CallbackRate(callbackRate string) *FuturesPlaceAlgoOrderRequest {
	f.callbackRate = &callbackRate
	return f
}

/*
 * ClientAlgoId sets
 */
func (f *FuturesPlaceAlgoOrderRequest) ClientAlgoId(clientAlgoId string) *FuturesPlaceAlgoOrderRequest {
	f.clientAlgoId = &clientAlgoId
	return f
}

/*
 * SelfTradePreventionMode sets
 */
func (f *FuturesPlaceAlgoOrderRequest) SelfTradePreventionMode(selfTradePreventionMode SelfTradePreventionModeType) *FuturesPlaceAlgoOrderRequest {
	f.selfTradePreventionMode = &selfTradePreventionMode
	return f
}

/*
 * GoodTillDate sets
 */
func (f *FuturesPlaceAlgoOrderRequest) GoodTillDate(goodTillDate int64) *FuturesPlaceAlgoOrderRequest {
	f.goodTillDate = &goodTillDate
	return f
}

// GetQueryParameters builds and checks the query parameters and returns url.Values
func (f *FuturesPlaceAlgoOrderRequest) GetQueryParameters() (url.Values, error) {
	var params = map[string]interface{}{}

	query := url.Values{}
	for _k, _v := range params {
		if f.isVarSlice(_v) {
			f.iterateSlice(_v, func(it interface{}) {
				query.Add(_k+"[]", fmt.Sprintf("%v", it))
			})
		} else {
			query.Add(_k, fmt.Sprintf("%v", _v))
		}
	}

	return query, nil
}

// GetParameters builds and checks the parameters and return the result in a map object
func (f *FuturesPlaceAlgoOrderRequest) GetParameters() (map[string]interface{}, error) {
	var params = map[string]interface{}{}
	// check algoType field -> json key algoType
	algoType := f.algoType

	// TEMPLATE check-required
	if len(algoType) == 0 {
	}
	// END TEMPLATE check-required

	// TEMPLATE check-valid-values
	switch algoType {
	case AlgoTypeConditional:
		params["algoType"] = algoType

	default:
		return nil, fmt.Errorf("algoType value %v is invalid", algoType)

	}
	// END TEMPLATE check-valid-values

	// assign parameter of algoType
	params["algoType"] = algoType
	// check symbol field -> json key symbol
	symbol := f.symbol

	// TEMPLATE check-required
	if len(symbol) == 0 {
	}
	// END TEMPLATE check-required

	// assign parameter of symbol
	params["symbol"] = symbol
	// check side field -> json key side
	side := f.side

	// TEMPLATE check-required
	// END TEMPLATE check-required

	// assign parameter of side
	params["side"] = side
	// check orderType field -> json key type
	orderType := f.orderType

	// TEMPLATE check-required
	if len(orderType) == 0 {
	}
	// END TEMPLATE check-required

	// TEMPLATE check-valid-values
	switch orderType {
	case FuturesAlgoOrderTypeStopMarket, FuturesAlgoOrderTypeTakeProfitMarket, FuturesAlgoOrderTypeStop, FuturesAlgoOrderTypeTakeProfit, FuturesAlgoOrderTypeTrailingStopMarket:
		params["type"] = orderType

	default:
		return nil, fmt.Errorf("type value %v is invalid", orderType)

	}
	// END TEMPLATE check-valid-values

	// assign parameter of orderType
	params["type"] = orderType
	// check positionSide field -> json key positionSide
	if f.positionSide != nil {
		positionSide := *f.positionSide

		// TEMPLATE check-required
		if len(positionSide) == 0 {
		}
		// END TEMPLATE check-required

		// TEMPLATE check-valid-values
		switch positionSide {
		case PositionSideLong, PositionSideShort, PositionSideBoth:
			params["positionSide"] = positionSide

		default:
			return nil, fmt.Errorf("positionSide value %v is invalid", positionSide)

		}
		// END TEMPLATE check-valid-values

		// assign parameter of positionSide
		params["positionSide"] = positionSide
	} else {
	}
	// check timeInForce field -> json key timeInForce
	if f.timeInForce != nil {
		timeInForce := *f.timeInForce

		// TEMPLATE check-required
		if len(timeInForce) == 0 {
		}
		// END TEMPLATE check-required

		// TEMPLATE check-valid-values
		switch timeInForce {
		case TimeInForceTypeGTC, TimeInForceTypeIOC, TimeInForceTypeFOK, TimeInForceTypeGTD:
			params["timeInForce"] = timeInForce

		default:
			return nil, fmt.Errorf("timeInForce value %v is invalid", timeInForce)

		}
		// END TEMPLATE check-valid-values

		// assign parameter of timeInForce
		params["timeInForce"] = timeInForce
	} else {
	}
	// check quantity field -> json key quantity
	if f.quantity != nil {
		quantity := *f.quantity

		// TEMPLATE check-required
		if len(quantity) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of quantity
		params["quantity"] = quantity
	} else {
	}
	// check price field -> json key price
	if f.price != nil {
		price := *f.price

		// TEMPLATE check-required
		if len(price) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of price
		params["price"] = price
	} else {
	}
	// check triggerPrice field -> json key triggerPrice
	if f.triggerPrice != nil {
		triggerPrice := *f.triggerPrice

		// TEMPLATE check-required
		if len(triggerPrice) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of triggerPrice
		params["triggerPrice"] = triggerPrice
	} else {
	}
	// check workingType field -> json key workingType
	if f.workingType != nil {
		workingType := *f.workingType

		// TEMPLATE check-required
		if len(workingType) == 0 {
		}
		// END TEMPLATE check-required

		// TEMPLATE check-valid-values
		switch workingType {
		case WorkingTypeMarkPrice, WorkingTypeContractPrice:
			params["workingType"] = workingType

		default:
			return nil, fmt.Errorf("workingType value %v is invalid", workingType)

		}
		// END TEMPLATE check-valid-values

		// assign parameter of workingType
		params["workingType"] = workingType
	} else {
	}
	// check priceMatch field -> json key priceMatch
	if f.priceMatch != nil {
		priceMatch := *f.priceMatch

		// TEMPLATE check-required
		if len(priceMatch) == 0 {
		}
		// END TEMPLATE check-required

		// TEMPLATE check-valid-values
		switch priceMatch {
		case PriceMatchNone, PriceMatchOpponent, PriceMatchOpponent5, PriceMatchOpponent10, PriceMatchOpponent20, PriceMatchQueue, PriceMatchQueue5, PriceMatchQueue10, PriceMatchQueue20:
			params["priceMatch"] = priceMatch

		default:
			return nil, fmt.Errorf("priceMatch value %v is invalid", priceMatch)

		}
		// END TEMPLATE check-valid-values

		// assign parameter of priceMatch
		params["priceMatch"] = priceMatch
	} else {
	}
	// check closePosition field -> json key closePosition
	if f.closePosition != nil {
		closePosition := *f.closePosition

		// TEMPLATE check-required
		// END TEMPLATE check-required

		// assign parameter of closePosition
		params["closePosition"] = closePosition
	} else {
	}
	// check priceProtect field -> json key priceProtect
	if f.priceProtect != nil {
		priceProtect := *f.priceProtect

		// TEMPLATE check-required
		if len(priceProtect) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of priceProtect
		params["priceProtect"] = priceProtect
	} else {
	}
	// check reduceOnly field -> json key reduceOnly
	if f.reduceOnly != nil {
		reduceOnly := *f.reduceOnly

		// TEMPLATE check-required
		// END TEMPLATE check-required

		// assign parameter of reduceOnly
		params["reduceOnly"] = reduceOnly
	} else {
	}
	// check activatePrice field -> json key activatePrice
	if f.activatePrice != nil {
		activatePrice := *f.activatePrice

		// TEMPLATE check-required
		if len(activatePrice) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of activatePrice
		params["activatePrice"] = activatePrice
	} else {
	}
	// check callbackRate field -> json key callbackRate
	if f.callbackRate != nil {
		callbackRate := *f.callbackRate

		// TEMPLATE check-required
		if len(callbackRate) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of callbackRate
		params["callbackRate"] = callbackRate
	} else {
	}
	// check clientAlgoId field -> json key clientAlgoId
	if f.clientAlgoId != nil {
		clientAlgoId := *f.clientAlgoId

		// TEMPLATE check-required
		if len(clientAlgoId) == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of clientAlgoId
		params["clientAlgoId"] = clientAlgoId
	} else {
	}
	// check selfTradePreventionMode field -> json key selfTradePreventionMode
	if f.selfTradePreventionMode != nil {
		selfTradePreventionMode := *f.selfTradePreventionMode

		// TEMPLATE check-required
		if len(selfTradePreventionMode) == 0 {
		}
		// END TEMPLATE check-required

		// TEMPLATE check-valid-values
		switch selfTradePreventionMode {
		case SelfTradePreventionModeExpireTaker, SelfTradePreventionModeExpireMaker, SelfTradePreventionModeExpireBoth, SelfTradePreventionModeNone:
			params["selfTradePreventionMode"] = selfTradePreventionMode

		default:
			return nil, fmt.Errorf("selfTradePreventionMode value %v is invalid", selfTradePreventionMode)

		}
		// END TEMPLATE check-valid-values

		// assign parameter of selfTradePreventionMode
		params["selfTradePreventionMode"] = selfTradePreventionMode
	} else {
	}
	// check goodTillDate field -> json key goodTillDate
	if f.goodTillDate != nil {
		goodTillDate := *f.goodTillDate

		// TEMPLATE check-required

		if goodTillDate == 0 {
		}
		// END TEMPLATE check-required

		// assign parameter of goodTillDate
		params["goodTillDate"] = goodTillDate
	} else {
	}

	return params, nil
}

// GetParametersQuery converts the parameters from GetParameters into the url.Values format
func (f *FuturesPlaceAlgoOrderRequest) GetParametersQuery() (url.Values, error) {
	query := url.Values{}

	params, err := f.GetParameters()
	if err != nil {
		return query, err
	}

	for _k, _v := range params {
		if f.isVarSlice(_v) {
			f.iterateSlice(_v, func(it interface{}) {
				query.Add(_k+"[]", fmt.Sprintf("%v", it))
			})
		} else {
			query.Add(_k, fmt.Sprintf("%v", _v))
		}
	}

	return query, nil
}

// GetParametersJSON converts the parameters from GetParameters into the JSON format
func (f *FuturesPlaceAlgoOrderRequest) GetParametersJSON() ([]byte, error) {
	params, err := f.GetParameters()
	if err != nil {
		return nil, err
	}

	return json.Marshal(params)
}

// GetSlugParameters builds and checks the slug parameters and return the result in a map object
func (f *FuturesPlaceAlgoOrderRequest) GetSlugParameters() (map[string]interface{}, error) {
	var params = map[string]interface{}{}

	return params, nil
}

var FuturesPlaceAlgoOrderRequestSlugReCache sync.Map

func (f *FuturesPlaceAlgoOrderRequest) applySlugsToUrl(url string, slugs map[string]string) string {
	for _k, _v := range slugs {
		var needleRE *regexp.Regexp

		if cached, ok := FuturesPlaceAlgoOrderRequestSlugReCache.Load(_k); ok {
			needleRE = cached.(*regexp.Regexp)
		} else {
			needleRE = regexp.MustCompile(":" + _k + "\\b")
			FuturesPlaceAlgoOrderRequestSlugReCache.Store(_k, needleRE)
		}

		url = needleRE.ReplaceAllString(url, _v)
	}

	return url
}

func (f *FuturesPlaceAlgoOrderRequest) iterateSlice(slice interface{}, _f func(it interface{})) {
	sliceValue := reflect.ValueOf(slice)
	for _i := 0; _i < sliceValue.Len(); _i++ {
		it := sliceValue.Index(_i).Interface()
		_f(it)
	}
}

func (f *FuturesPlaceAlgoOrderRequest) isVarSlice(_v interface{}) bool {
	rt := reflect.TypeOf(_v)
	switch rt.Kind() {
	case reflect.Slice:
		return true
	}
	return false
}

func (f *FuturesPlaceAlgoOrderRequest) GetSlugsMap() (map[string]string, error) {
	slugs := map[string]string{}
	params, err := f.GetSlugParameters()
	if err != nil {
		return slugs, nil
	}

	for _k, _v := range params {
		slugs[_k] = fmt.Sprintf("%v", _v)
	}

	return slugs, nil
}

// GetPath returns the request path of the API
func (f *FuturesPlaceAlgoOrderRequest) GetPath() string {
	return "/fapi/v1/algoOrder"
}

// Do generates the request object and send the request object to the API endpoint
func (f *FuturesPlaceAlgoOrderRequest) Do(ctx context.Context) (*FuturesPlaceAlgoOrderResponse, error) {

	params, err := f.GetParameters()
	if err != nil {
		return nil, err
	}
	query := url.Values{}

	var apiURL string

	apiURL = f.GetPath()

	req, err := f.client.NewAuthenticatedRequest(ctx, "POST", apiURL, query, params)
	if err != nil {
		return nil, err
	}

	response, err := f.client.SendRequest(req)
	if err != nil {
		return nil, err
	}

	var apiResponse FuturesPlaceAlgoOrderResponse

	type responseUnmarshaler interface {
		Unmarshal(data []byte) error
	}

	if unmarshaler, ok := interface{}(&apiResponse).(responseUnmarshaler); ok {
		if err := unmarshaler.Unmarshal(response.Body); err != nil {
			return nil, err
		}
	} else {
		// The line below checks the content type, however, some API server might not send the correct content type header,
		// Hence, this is commented for backward compatibility
		// response.IsJSON()
		if err := response.DecodeJSON(&apiResponse); err != nil {
			return nil, err
		}
	}

	type responseValidator interface {
		Validate() error
	}

	if validator, ok := interface{}(&apiResponse).(responseValidator); ok {
		if err := validator.Validate(); err != nil {
			return nil, err
		}
	}
	return &apiResponse, nil
}
